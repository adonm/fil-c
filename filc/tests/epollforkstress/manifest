return:
  success
slow:
  true

# COSMO: the nsync fork panic this mark used to describe is fixed (the fork child's filc runtime now tolerates nsync_waiter_wipe_ having zeroed thread locks), and the test now runs many fork levels deep with live threads - but it still hangs intermittently in a futex wait inside the fd-table lock (filc/src/runtime.c lock_lock) under heavy thread+fork stress; also very slow in this flavor.
unsupportedOnCOSMO: true
